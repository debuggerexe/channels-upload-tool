# 微信视频号自动上传工具

自动化发布视频到微信视频号平台，支持本地和 Notion 云端两种数据源模式。

## 功能特性

- **双数据源模式**
  - **本地模式**：从 `videos/` 文件夹读取 txt 文件获取标题/标签
  - **云端模式**：从 Notion 数据库读取视频元数据，本地只存视频文件
  - **智能匹配**：自动匹配 Notion 记录与本地视频文件

- **智能处理**
  - 智能文件名匹配（自动去除日期前缀）
  - 短标题自动清理（过滤微信不支持的特殊字符，**超过16字自动截断**）
  - 封面智能裁剪（高图自动裁剪为3:4比例）
  - 自动声明原创
  - **自动添加合集（匹配成功才添加）**
  - **并行上传优化（上传期间填写表单，节省时间）**

- **批量上传**
  - 按发布日期自动排序
  - 失败自动重试
  - 上传成功后自动归档到 `published/` 目录

---

## 快速开始

```bash
source .venv/bin/activate && python upload_wechat_videos.py
```

运行后选择数据源模式：
1. **本地模式** - 从 `videos/` 读取 txt 文件获取标题/标签
2. **云端模式** - 从 Notion 读取元数据

### 上传流程优化

工具采用**并行处理**策略节省上传时间：

1. **开始上传** - 选择视频文件开始上传
2. **并行处理** - 视频上传期间自动填写：
   - 标题和话题
   - 短标题
   - 合集选择
   - 原创声明
   - 位置设置（不显示位置）
3. **封面处理** - 视频上传完成后处理封面（必须等视频解析完成）
4. **发布/保存** - 点击发表或保存草稿

此优化充分利用视频上传的等待时间，大幅提升批量上传效率。

---

## 新机器初始化

首次在新机器上运行：

```bash
# 1. 克隆项目
git clone https://github.com/debuggerexe/channels-upload-tool.git
cd channels-upload-tool

# 2. 创建虚拟环境
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate

# 3. 安装依赖
pip install -r requirements.txt
playwright install

# 4. 复制配置文件（必须步骤）
cp config.example.json config.json
# 根据需要编辑 config.json
```

**首次运行（获取 Cookie）：**

```bash
source .venv/bin/activate && python upload_wechat_videos.py
```

会弹出二维码扫码登录，登录成功后 cookie 自动保存，程序会继续执行上传流程。

**之后每次运行：**

```bash
source .venv/bin/activate && python upload_wechat_videos.py
```

Cookie 已保存，直接开始上传，无需再次扫码。

---

### 数据源说明

**本地模式 (TXT)**：
- 从 `videos/文件夹/*.txt` 读取标题和描述
- 适合个人独立使用

**云端模式 (Notion)**：
- 从 Notion 数据库读取视频元数据
- 适合团队协作，内容管理更方便
- 在配置管理页面 (`/config`) 配置 API Token 和数据库信息
- 系统会自动匹配 Notion 记录与本地视频文件

**数据合并策略**：
- 如果配置了 Notion Token，优先从 Notion 获取标题、描述、标签等元数据
- 本地 TXT 文件作为回退
- 封面图、发布配置等仍从本地或 config.json 读取
- 数据来源会在视频预览卡片中显示（本地/云端）

选择本地模式后，会提示选择操作：

1. **定时发布** - 确认并开始上传，按设置的发布时间发表视频
2. **保存草稿** - 确认并开始上传，仅保存到草稿箱
3. **取消上传** - 退出程序

---

### 云端模式发布方式

选择云端模式后，会提示选择发布方式：

1. **定时发布** - 按设置的发布时间发表视频
2. **保存草稿** - 仅保存到草稿箱，不发表
3. **退出** - 取消上传

---

## 本地模式

### 目录结构

```
videos/
├── 260101_video1/
│   ├── video1.mp4          # 视频文件
│   ├── video1.txt          # 标题和描述（可选）
│   └── cover1.jpg          # 封面图（可选）
└── 260102_video2/
    ├── video2.mp4
    ├── video2.txt
    └── cover2.jpg
```

### txt 文件格式

在视频文件夹中创建与视频同名的 `.txt` 文件（如 `video1.txt`），格式如下：

```
视频短标题（第一行）
视频描述内容...
#话题标签1 #话题标签2
```

**填写要求：**

| 行 | 内容 | 必填 | 说明 |
|---|---|---|---|
| 第1行 | 短标题 | ✅ | 视频号顶部显示的短标题，**只保留中文字符** |
| 第2-N行 | 描述 | 可选 | 视频描述区内容，支持多行 |
| 最后1行 | 标签 | 可选 | 以 `#` 开头的话题标签 |

**短标题处理规则：**
- 只保留中文字符（自动去掉英文、数字、符号）
- 不足6字自动补空格
- **超过16字强行截断**（直接取前16字）

**描述区格式：**

视频号描述区最终内容 = `标题 + 换行 + 描述 + 换行 + 标签`

- 如果描述最后一行已有以 `#` 开头的标签，不再追加标签
- 如果没有标签，仅显示短标题和描述

---

## 云端模式（Notion）

### 数据库字段

| 字段 | 类型 | 说明 | 必填 |
|------|------|------|------|
| `短标题` | `Title` | 视频号短标题（16字以内），同时用于匹配本地视频文件名/文件夹名 | ✅ |
| `标题` | `Rich Text` | 视频号主标题（显示在视频下方） | 可选 |
| `描述` | `Rich Text` | 视频描述区内容 | 可选 |
| `标签` | `Rich Text` | 话题标签（如 `#话题1 #话题2`） | 可选 |
| `合集` | `Multi-select` | 视频合集名称列表（可多选） | 可选 |
| `发布日期` | `Date` | 发布日期 | ✅ |

**字段说明：**
- **短标题**：视频号顶部显示的短标题（16字以内），同时用于匹配本地视频文件
  - 只保留中文字符（自动去掉英文、数字、符号）
  - 不足6字自动补空格
  - 超过16字**强行截断**（直接取前16字，不保留完整词义）
- **标题**：视频下方显示的主标题
- **描述**：视频描述区内容
- **标签**：话题标签，会自动追加到描述区后面
- **合集**：视频合集名称列表（支持多选）
  - Notion 中使用 `Multi-select` 类型，可添加多个合集
  - 上传时自动点击"选择合集"按钮
  - **匹配规则**：Notion 中的任意合集名称与视频号平台的任意合集**完全匹配**即可自动选择
  - 全部未匹配到会提示在视频号后台手动创建
- **发布日期**：发布日期，可只填日期（用 `publish_times` 作为时间）或完整时间

**视频号描述区格式：**

格式：`标题 + 换行 + 描述 + 换行 + 标签`

| 部分 | 来源字段 | 说明 |
|------|----------|------|
| 标题 | `标题` 字段 | 视频号主标题，显示在描述区第一行 |
| 描述 | `描述` 字段 | 视频详细描述内容 |
| 标签 | `标签` 字段 | 以 `#` 开头的话题标签 |

**标签去重逻辑：**
- 如果**描述字段的最后一行以 # 开头**（表示已包含话题标签），则不再追加标签字段
- 如果描述字段中没有标签行，且有标签字段，则将标签追加到描述后面

**示例1（描述已含标签）：**
```
标题: "啊啊啊"
描述: "啊啊啊啊啊...\n#标签1 #标签2"
标签: "#标签1 #标签2"
```
→ 最终描述区内容：
```
啊啊啊
啊啊啊啊啊...
#标签1 #标签2
```
（标签字段被忽略，因为描述中已有标签）

**示例2（描述不含标签）：**
```
标题: "啊啊啊"
描述: "啊啊啊啊啊..."
标签: "#标签1 #标签2"
```
→ 最终描述区内容：
```
啊啊啊
啊啊啊啊啊...
#标签1 #标签2
```

### 视频存放方式

**方式一：子文件夹形式（推荐）**
```
videos/
├── 260101_video1/
│   ├── video1.mp4
│   └── cover1.jpg
└── 260102_video2/
    ├── video2.mp4
    └── cover2.jpg
```

**方式二：直接存放**
```
videos/
├── 260101_video1.mp4
├── 260101_video1.jpg
├── 260102_video2.mp4
└── 260102_video2.jpg
```

### 环境变量

```bash
export NOTION_API_TOKEN='your_token'
```

---

## 配置说明

**配置文件优先级：**
1. `config.json` - 本地配置文件（优先读取）
2. `config.example.json` - 默认配置模板（随仓库提供）

**首次使用：**

```bash
# 复制示例配置文件
cp config.example.json config.json

# 根据需要编辑配置
nano config.json
```

**config.json 示例：**

```json
{
  "publish_date": "2026-01-01",
  "publish_times": ["10:00"],
  "original_declaration": true,
  "cover_position": "top",
  "collection": ""
}
```

**配置项说明：**

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `publish_date` | string | "2026-01-01" | 默认发布日期，格式：YYYY-MM-DD，无日期前缀的文件夹使用 |
| `publish_times` | array | ["10:00"] | 每日发布时间，可设多个时段如 ["10:00", "14:00"] |
| `timezone` | string | "Asia/Shanghai" | 时区设置 |
| `video_dir` | string | "videos" | 视频文件夹路径 |
| `text_dir` | string | "texts" | 文本文件夹路径 |
| `original_declaration` | boolean | true | 是否自动声明原创：true / false |
| `cover_position` | string | "top" | 封面裁剪位置：top(顶部) / middle(中间) / bottom(底部) |
| `collection` | string | "" | 默认合集名称，留空则不添加，支持逗号分隔多个 |
| `notion_api_token` | string | "" | Notion API Token（用于云端模式） |
| `notion_database_id` | string | "" | Notion数据库ID（云端模式可选） |
| `notion_database_name` | string | "" | Notion数据库名称（云端模式可选） |

完整配置字段见 `config.example.json`

---

## 手动上传（调试用）

```bash
python open_browser.py
```

打开已登录的浏览器，可手动操作上传。

---

## Cookie 获取

首次使用需获取登录凭证：

```bash
python examples/get_tencent_cookie.py
```

扫码登录后会自动保存到 `cookies/tencent_uploader/account.json`

---

## 代码架构优化

### 统一上传流程

**重构前的问题：**
- 本地模式和云端模式有 100+ 行重复代码
- 上传逻辑分散在两个方法中
- 维护困难，容易遗漏功能同步

**重构后的结构：**

```python
# 公共方法（统一处理）
_upload_single_video()     # 单视频上传（含重试、封面处理）
_move_to_published()       # 文件归档到 published 目录

# 模式入口（仅数据组装和流程控制）
upload_all_videos()        # 本地模式 → 调用公共方法
upload_videos_from_source() # 云端模式 → 调用公共方法
```

**优化收益：**
- 代码量减少约 60%
- 两种模式功能完全一致（发布方式、短标题截断、封面处理）
- 维护简单，只需修改一处即可同步到两种模式

### 封面处理优化

**问题：** 视频上传完成后，封面预览元素可能还未就绪，导致封面处理失败。

**解决：** 添加封面预览等待逻辑，最多等待 10 秒确保封面元素加载完成后再处理。

---

## 项目结构

```
├── data_sources/           # 数据源模块
│   ├── data_source.py      # 抽象基类
│   ├── local_data_source.py  # 本地实现
│   └── notion_data_source.py # Notion 实现
├── uploader/
│   └── tencent_uploader/   # 微信视频号上传器
│       └── main.py
├── utils/                  # 工具模块
│   ├── cover_cropper.py    # 封面裁剪
│   ├── files_times.py      # 文件读取、时间处理
│   └── log.py              # 日志
├── videos/                 # 视频文件夹
├── published/              # 已上传视频归档
├── upload_wechat_videos.py # 批量上传主程序
├── open_browser.py         # 手动上传工具
├── config.example.json     # 配置模板（复制为 config.json 后修改）
└── config.json            # 本地配置文件（不上传 GitHub）
```

---

## 依赖安装

```bash
pip install -r requirements.txt
playwright install
```

---

## 日志

日志文件保存在 `logs/tencent.log`

---

## 🎨 Web 可视化界面

项目现在支持可视化 Web 界面，让普通用户可以方便地管理和上传视频。

### 功能特性

- **控制台仪表盘** - 实时查看视频统计、启动/停止上传任务、实时查看日志
- **配置管理** - 可视化编辑 config.json，支持实时保存
- **视频预览** - 小红书风格卡片式展示，支持：
  - 3:4 封面图预览
  - 卡片内直接播放视频（自适应高度）
  - 显示完整视频信息（短标题、描述、话题标签）
  - 显示发布配置（合集、封面位置、原创声明、发布时间）
  - 支持本地 TXT 和 Notion 云端数据源
- **Notion 集成** - 云端数据源的设置说明
- **实时日志** - WebSocket 推送上传日志到浏览器

### 技术栈

- **前端**: Next.js 14 + React 18 + TypeScript + Tailwind CSS + shadcn/ui
- **后端**: FastAPI + WebSocket
- **通信**: REST API + WebSocket 实时推送

### 启动方式

**1. 安装前端依赖**

```bash
cd web
npm install
```

**2. 启动后端 API**

```bash
source .venv/bin/activate
python -m api.main
```

**3. 启动前端（新终端）**

```bash
cd web
npm run dev
```

**4. 访问界面**

打开浏览器访问 http://localhost:3000

### 界面路由

- **控制台** (`/`) - 查看视频统计、启动上传任务、实时查看日志
- **视频预览** (`/videos`) - 浏览 videos/ 文件夹中的所有视频
  - 封面悬停显示播放按钮
  - 点击播放，点击视频外区域关闭
  - **数据源切换开关** - 支持本地/云端/全部三种模式切换
  - 显示视频号平台对应的所有字段信息：
    - 视频文件名称、发布时间、位置、合集、定时发布
    - 短标题、原创声明
    - 本地 TXT 数据（标题、描述、标签）
    - 云端 Notion 数据（标题、描述、标签）
    - 数据来源标识（本地/云端/两者）
- **配置管理** (`/config`) - 编辑上传配置参数，支持：
  - 可视化编辑 config.json
  - Notion API Token 配置和测试连接
  - 实时保存配置
  - 配置验证
- **Notion 集成** (`/notion`) - 云端数据源设置指南

---

## 项目结构

```
├── api/                    # FastAPI 后端
│   ├── main.py            # API 入口
│   └── routers/           # API 路由
│       ├── config.py     # 配置管理
│       ├── videos.py     # 视频扫描
│       └── upload.py     # 上传控制
├── web/                    # Next.js 前端
│   ├── app/              # 页面路由
│   │   ├── page.tsx      # 控制台
│   │   ├── videos/       # 视频预览
│   │   ├── config/       # 配置管理
│   │   └── notion/       # Notion 集成
│   └── components/       # UI 组件
├── data_sources/          # 数据源模块
├── uploader/              # 微信视频号上传器
├── utils/                 # 工具模块
├── videos/                # 视频文件夹
├── published/             # 已上传视频归档
└── config.json            # 本地配置文件
```

## 更新日志

### 2024-04-06 (健壮性优化)
- **Web 控制台改进** - 智能滚动检测，用户查看历史时暂停自动滚动
- **Git 配置完善** - 更新 .gitignore，添加 Node.js/Web 项目忽略规则
- **错误处理增强** - 优化 KeyboardInterrupt 处理，登录失败友好提示
- **配置读取健壮性** - 添加 JSON 解析错误处理，防止配置文件格式错误导致崩溃
- **Notion Token 读取优化** - 优先从 config.json 读取，其次环境变量

### 2024-04-06 (安全修复)
- **清理敏感信息** - 移除硬编码的个人化内容
  - 从 Git 仓库移除 config.json（已在 .gitignore 中，保留本地文件）
  - 清空默认配置中的敏感信息
  - 将默认配置中的个人化字符串改为空值（由用户自行配置）
  - 将代码注释中的个性化示例改为中性示例

### 2024-04-06 (配色重设计)
- **全新配色方案** - 从紫罗兰系改为青绿-蓝色系，更简洁现代
  - 主色: Teal-500 (#14B8A6) + Cyan-500 (#06B6D4) + Sky-500 (#0EA5E9)
  - 深色背景: Slate-950 (#020617) + Slate-900 (#0f172a)
  - 文字层级: Slate-100 主文字 / Slate-400 次要文字 / Slate-500 辅助文字
  - 卡片边框统一使用 slate-700/50，更柔和
  - 按钮、标签、进度条全部改用青绿渐变
  - 移除了过于鲜艳的紫罗兰和粉色调

### 2024-04-06
- **完整 UI 重设计** - 基于专业创作者工具风格，全面升级网站设计
  - **全新深色主题** - 深蓝黑色背景 (#030508) + 紫靛蓝渐变强调色
  - **全局样式系统** - Liquid Glass 玻璃态效果、渐变文字、发光效果
  - **侧边栏重设计** - 浮动导航 + 图标渐变 + 活跃状态指示器 + 平滑动画
  - **视频管理页面** - 
    - 统一卡片设计，带玻璃态背景和悬停效果
    - 重新设计的数据源选择页面，居中布局 + 大图标
    - 优化上传控制面板，紫罗兰渐变按钮
    - 控制台带彩色日志和动画指示器
    - 视频卡片简化信息展示，更好的视觉层级
  - **配置管理页面** - 玻璃态卡片 + 彩色图标 + 保存动画
  - **Notion 集成页面** - 三步快速开始 + 字段说明网格 + 双栏优势对比
  - **动效系统** - Framer Motion 入场动画、悬停效果、页面切换
  - **排版对齐** - 统一的间距系统 (8px 网格)、一致的圆角 (rounded-2xl)

### 2024-04-05
- **前端界面美化** - 使用 `frontend-dev` skill 全面升级 UI 设计
  - 新增玻璃态效果 (Glass Morphism) 和渐变背景
  - 添加 Framer Motion 动画（卡片悬停、页面切换、进度条动画）
  - 优化控制台设计，带发光效果和彩色日志
  - 数据源选择页面全新设计，带图标和动画
  - 视频卡片添加悬停抬升效果和播放动画
  - 按钮添加渐变色彩和阴影效果
- **配置管理页面美化** - 全面升级配置页面 UI
  - 添加渐变背景和玻璃态卡片设计
  - 为每个配置区块添加彩色图标和标签
  - 保存成功后添加滑入动画提示
  - Notion 连接测试结果添加动画效果，支持定时发布和保存草稿两种模式
- **实时控制台** - 页面顶部整合控制台输出，实时显示上传进度和日志
- **上传状态监控** - 支持停止上传任务，显示当前处理视频和进度条
- **Notion 封面调整字段** - 支持 select 类型（top/middle/bottom），默认 middle
- **云端模式优化** - 云端模式下封面调整使用 Notion 配置
- **视频预览页面** - 添加错误处理和加载状态显示
- **修复云端数据加载** - 修复 Name 字段匹配和 checkbox 类型支持
- **修复变量未定义错误** - 修复 original_declaration 和 config_collection 变量
- **修复云端模式匹配** - 添加 name_for_match 字段用于正确匹配本地视频和 Notion 记录
- **修复本地/云端模式筛选** - 本地模式显示所有视频，云端模式只显示有 Notion 数据的视频
- **严格数据源分离** - 本地模式只加载本地数据，云端模式只加载与本地匹配的Notion数据
- **云端视频预览** - 云端模式使用本地视频文件播放
- **云端日期处理** - 发布日期只有日期时默认09:00，日期为空时提示用户填写
